// import untested service for code-coverage
import {ServerModuleLoader} from './server-module.loader';

for (const a in [
    ServerModuleLoader
]) {
    console.log('import unused modules for codecoverage');
}
